// THE TWIN THE CHAIN NEVER REACHES.
//
// Same package name, same version, same vulnerable export name as the
// nested install inside vt2-chain-lib. It is a real declared dependency of
// this application, so it is a real installed instance with its own real
// finding -- it is simply not the one any code here ever calls.
function dangerousOp(input) {
  return reachedSinkTopLevel(input);
}

function reachedSinkTopLevel(input) {
  return "top-level-instance:" + input;
}

exports.dangerousOp = dangerousOp;
