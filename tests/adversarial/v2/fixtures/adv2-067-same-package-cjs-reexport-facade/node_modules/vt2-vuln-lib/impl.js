'use strict';

// The only file in this package that declares anything.
function dangerousOp(input) {
  return 'danger:' + input;
}

function safeOp(input) {
  return 'safe:' + input;
}

exports.dangerousOp = dangerousOp;
exports.safeOp = safeOp;
