'use strict';

// A DECOY inside the same package: a function with the exact target name
// that nothing exports and nothing re-exports. A bare same-file/same-package
// name search would bind the rule target here.
function dangerousOp(input) {
  return 'decoy:' + input;
}

module.exports = { neverExported: dangerousOp };
