'use strict';

// A facade that declares NOTHING. Every advertised export is assembled from
// require()d siblings, in the real-world `var` + shorthand spelling.
var ops = require('./internal/ops');
var decoy = require('vt2-decoy-lib');

module.exports = {
  // Reaches ./internal/impl.js's real dangerousOp, two hops away.
  dangerousOp: ops.dangerousOp,
  safeOp: ops.safeOp,
  // A DIFFERENT package's identically-named function. Chasing this is
  // cross-package re-export, which is not implemented -- it must never be
  // bound to this package's own rule target.
  borrowedOp: decoy.dangerousOp,
};
