"use strict";

// The vulnerable export named by the advisory. This file exists TWICE, at
// two different install paths, identical in name, version and export name.
function dangerousOp(input) {
  return "danger:" + input;
}

module.exports = { dangerousOp };
