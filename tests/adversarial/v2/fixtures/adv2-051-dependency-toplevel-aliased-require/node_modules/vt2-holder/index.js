const lib = require("vt2-vuln-lib");

// TOP-LEVEL (module-scope) ALIASED require -- `r` is a local binding whose
// value is exactly the real `require` function, called indirectly with a
// runtime-only specifier. This is VT-307b's own regression case: before
// VT-307b, this was classified as a plain `unsupported_construct` (a
// catch-all bucket), which is NON-closure-widening -- so this real
// module-loading capability was invisible to VT-300's guard even after
// VT-307a's module-load edges made the surrounding module scope reachable
// at all. It executes the moment this module is loaded, never inside any
// function, and could load ANY installed module, including the separate,
// never-imported top-level vt2-vuln-lib@1.9.0 instance.
const r = require;
const pluginName = process.env.ADV2_051_PLUGIN;
r(pluginName);

function useHolder() {
  return lib.dangerousOp();
}

module.exports = { useHolder };
