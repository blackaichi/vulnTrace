const Module = require("module");
const lib = require("vt2-vuln-lib");

// TOP-LEVEL (module-scope) loader-primitive call reached through a
// PROVENANCE ALIAS -- `Module.prototype` (a capability-bearing VALUE, not
// a call target) is held in a local const, then used TWO STEPS later
// (`.constructor`, then `._preloadModules`) to reach the same static
// loading primitive `Module._preloadModules(...)` would reach directly.
//
// This fixture pins the DEFECT CLASS the final invariant certification
// found in the CLASSIFIER'S OWN PROVENANCE-RESOLUTION layer, not in the
// value-flow layer VT-307c-value-flow-closure already closed: the
// alias-EXEMPTION relation (which decides a `const proto = Module.
// prototype;` declaration is safe to leave untouched, since it merely
// creates an alias) was BROADER than what the classifier's OWN use-site
// resolvers could resolve `proto` back to. `Module.prototype` was
// neither a recognized capability VALUE (so it never escaped at its own
// declaration) nor a resolvable RECEIVER through an alias (so
// `proto.constructor` was never recognized as reaching the Module
// constructor again) -- it fell between both layers entirely, reproduced
// end-to-end by the final certification with NO unknown Node API name
// and NO novel value-container syntax involved anywhere: `Module.
// prototype` and `.constructor` are both spellings this classifier had
// ALREADY modeled for years, just never through this one alias hop.
//
// VT-307c-provenance-closure closes this by making `Module.prototype`
// itself a first-class, independently resolvable capability KIND in a
// single shared relation every receiver/value/callee check in the
// classifier now consults, so an alias of it resolves through the exact
// same code path a direct `Module.prototype.constructor...` access does.
// Before that fix this construct was invisible entirely (not even an
// unsupported_construct edge) despite being built from two spellings the
// classifier had separately understood since VT-307c-fix-6. It runs the
// moment this module is loaded, never inside any function, and the
// analyzer can never rule out that the constructor reached this way is
// used to load a different installed module, including the separate,
// never-imported top-level vt2-vuln-lib@1.9.0 instance.
const proto = Module.prototype;
proto.constructor._preloadModules(["vt2-vuln-lib"]);

function useHolder() {
  return lib.dangerousOp();
}

module.exports = { useHolder };
