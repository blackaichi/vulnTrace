'use strict';

// The vulnerable implementation: the whole module IS the callable, and it
// is ANONYMOUS. So nothing in this package other than the stale
// declaration in index.js carries the name "dangerousOp" -- which is what
// leaves a name-based search with nothing better to bind.
module.exports = function (input) {
  return 'danger:' + String(input);
};
