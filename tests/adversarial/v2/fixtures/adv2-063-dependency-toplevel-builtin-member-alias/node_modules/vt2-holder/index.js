const cp = require("child_process");
const lib = require("vt2-vuln-lib");

// TOP-LEVEL (module-scope) child-process launch reached through a STORED
// MEMBER VALUE of a modeled Node builtin -- `child_process.fork` is held
// in a local const, then called one line later.
//
// This fixture pins the defect class the final certification found in
// VT-307c-provenance-closure itself. That task built ONE shared
// provenance relation and correctly routed every `module`-family
// resolver through it -- closing all twelve of its own blockers -- but
// left a SECOND relation standing beside it: `referencesBuiltinExport`
// was never folded in, kept its own one-hop, identifier-only alias
// logic, and serves every `BUILTIN_MEMBER_REASONS` entry. Because the
// shared relation modeled only the `module` builtin, the other three
// modeled builtins (`vm`, `child_process`, `worker_threads`) fell
// through BOTH relations. The result was the exact same defect the task
// had just eliminated for `Module._preloadModules`, surviving untouched
// for three builtins -- reproduced end-to-end with NO unknown Node API
// name involved anywhere, and with the DIRECT spelling (`cp.fork(...)`)
// and the NAMED-IMPORT spelling (`const { fork } = require(...)`) both
// proven to work correctly, so the hazard was already fully modeled and
// only the one alias hop lost it.
//
// VT-307c-builtin-closure closes this by generalizing the shared
// relation to all four modeled builtins and making
// `referencesBuiltinExport` a thin wrapper over it, so there is now
// genuinely one relation rather than two. The launch below runs the
// moment this module is loaded, never inside any function, and the
// analyzer can never rule out that the launched process loads a
// different installed module, including the separate, never-imported
// top-level vt2-vuln-lib@1.9.0 instance.
const launch = cp.fork;
launch(require("path").join(__dirname, "worker.js"));

function useHolder() {
  return lib.dangerousOp();
}

module.exports = { useHolder };
