// The vulnerable package the advisory names. Reached at runtime through
// vt2-cond-lib's `fastPath` branch, on every load where VT2_MODE is
// "fast".
function dangerousOp(input) {
  return reachedSink(input);
}

function reachedSink(input) {
  return "reached-sink:" + input;
}

exports.dangerousOp = dangerousOp;
