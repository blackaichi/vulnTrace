'use strict';

// A facade that declares NOTHING and offers no name to match on: the whole
// callable API is forwarded from a require()d sibling inside this same
// installed package.
module.exports = require('./internal/ops');

// A DIFFERENT installed package's identically-shaped anonymous export,
// hung off the same exported value. Chasing this is cross-package
// re-export, which is not implemented -- it must never be bound to this
// package's own rule target.
module.exports.borrowed = require('vt2-decoy-lib');
