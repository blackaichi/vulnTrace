'use strict';

// A SECOND anonymous module.exports function in the SAME installed
// package, which nothing requires and nothing re-exports. A mechanism that
// binds "an anonymous exported function somewhere in this package" rather
// than THE one the re-export chain actually reaches would land here.
module.exports = function (input) {
  return 'never-exported:' + input;
};
