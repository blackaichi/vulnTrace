const lib = require("vt2-vuln-lib");

// TOP-LEVEL (module-scope) module._compile(...) -- Node's own primitive
// underneath require() itself: compiles `code` AS IF it were this module's
// own source and executes it immediately with a live require/module/exports
// bound to the real module (VT-307c-fix-7's own regression case, mirroring
// ADV2-050/051/052/053's shape for the widening constructs VT-307a/VT-307b/
// VT-307c-fix-5/fix-6 already added). Before VT-307c-fix-7, this was
// invisible entirely -- not even an `unsupported_construct` edge -- so this
// real code-execution capability went completely undetected. It executes
// the moment this module is loaded, never inside any function, and could
// require/load ANY installed module at runtime, including the separate,
// never-imported top-level vt2-vuln-lib@1.9.0 instance.
const pluginName = process.env.ADV2_054_PLUGIN;
module._compile(
  `require(${JSON.stringify(pluginName)});`,
  __filename,
);

function useHolder() {
  return lib.dangerousOp();
}

module.exports = { useHolder };
