const lib = require("vt2-vuln-lib");

// TOP-LEVEL (module-scope) module.constructor._load(...) -- Node's own
// Module-constructor-level loading primitive underneath require() itself,
// reached through the ambient `module` object's own `.constructor`
// (VT-307c-fix-6's own regression case, mirroring ADV2-050/051/052's shape
// for the widening constructs VT-307a/VT-307b/VT-307c-fix-5 already added).
// Before VT-307c-fix-6, this was invisible entirely -- not even an
// `unsupported_construct` edge -- so this real module-loading capability
// went completely undetected. It executes the moment this module is
// loaded, never inside any function, and could load ANY installed module,
// including the separate, never-imported top-level vt2-vuln-lib@1.9.0
// instance.
const pluginName = process.env.ADV2_053_PLUGIN;
module.constructor._load(pluginName);

function useHolder() {
  return lib.dangerousOp();
}

module.exports = { useHolder };
