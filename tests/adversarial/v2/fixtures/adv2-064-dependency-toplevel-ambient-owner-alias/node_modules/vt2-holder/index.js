const lib = require("vt2-vuln-lib");

// TOP-LEVEL (module-scope) use of Node's own module-loading primitive,
// reached through an ALIASED AMBIENT OWNER -- `process` is held in a
// local const, and the entry Module instance is then reached through it
// (`p.mainModule.constructor`) rather than through the literal
// `process.mainModule.constructor` spelling.
//
// This fixture pins the defect class the certification found AFTER
// VT-307c-builtin-closure: the shared provenance relation had by then
// been generalized to every modeled builtin and every capability VALUE
// form, but an ambient chain's OWNER position (`require` in
// `require.main`, `process` in `process.mainModule`) was STILL matched by
// literal identifier name and never resolved. So aliasing the owner one
// hop -- or prefixing it with `globalThis.`/`global.`, both semantically
// identity operations -- lost the capability entirely, even though the
// unprefixed, unaliased spelling of the very same chain had been
// classified correctly since VT-307c-fix-7. Five such spellings were
// reproduced end-to-end, with NO unknown Node API name and NO novel
// value-container syntax involved anywhere.
//
// VT-307c-ambient-closure closes this by making `process` and the global
// object resolvable KINDS of the shared relation, so every way of
// reaching an ambient chain composes through the ordinary recursion
// instead of a name match. The call below runs the moment this module is
// loaded, never inside any function, and directly loads a package by
// name using Node's own primitive.
const p = process;
p.mainModule.constructor._preloadModules(["vt2-vuln-lib"]);

function useHolder() {
  return lib.dangerousOp();
}

module.exports = { useHolder };
