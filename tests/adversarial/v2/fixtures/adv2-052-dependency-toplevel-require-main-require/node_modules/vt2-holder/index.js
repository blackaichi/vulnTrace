const lib = require("vt2-vuln-lib");

// TOP-LEVEL (module-scope) require.main.require(...) -- a Node runtime
// primitive that loads a module through the ENTRYPOINT's own `require`
// cache, reached via an unusual property path (VT-307c-fix-5's own
// regression case, mirroring ADV2-050/ADV2-051's shape for the two
// widening constructs VT-307a/VT-307b added). Before VT-307c-fix-5, this
// was invisible entirely -- not even an `unsupported_construct` edge, let
// alone a closure-widening one -- so this real module-loading capability
// went completely undetected. It executes the moment this module is
// loaded, never inside any function, and could load ANY installed module,
// including the separate, never-imported top-level vt2-vuln-lib@1.9.0
// instance.
const pluginName = process.env.ADV2_052_PLUGIN;
require.main.require(pluginName);

function useHolder() {
  return lib.dangerousOp();
}

module.exports = { useHolder };
