// VT-303 fixture-authored wrapper (NOT a real published npm package).
// require("url-parse") from HERE resolves to the NESTED
// node_modules/consumer/node_modules/url-parse@1.4.7 instance -- Node's
// own module resolution always checks the nearest node_modules first --
// fully independent of the fixture's own top-level url-parse@1.4.4
// instance. No npm alias is involved anywhere in this fixture.
const Url = require("url-parse");

function parseNested(rawUrl) {
  return new Url(rawUrl);
}

module.exports = { parseNested };
