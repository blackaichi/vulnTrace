# VulnTrace Real-World CVE Validation Report

Generated by `npm run test:validation` (`tests/validation/validation.test.ts`). Expected verdicts come from `tests/validation/cases/cases.json`, authored independently of VulnTrace's own output by reading each case's real advisory and real vulnerable code path. Uses the real, live OSV API -- not stubbed.

## Summary

- Total cases: 17
- Passed: 11
- Failed: 6
  - Known (tracked in FINDINGS.md): 6
  - Unexpected: 0

## Result table

```
ID       EXPECTED        ACTUAL          RESULT
------------------------------------------------
RWB-01   AFFECTED        AFFECTED        PASS
RWB-02   AFFECTED        AFFECTED        PASS
RWB-03   AFFECTED        UNKNOWN         FAIL (known)
RWB-04   AFFECTED        AFFECTED        PASS
RWB-05   NOT_AFFECTED    UNKNOWN         FAIL (known)
RWB-06   NOT_AFFECTED    NOT_AFFECTED    PASS
RWB-06A  NOT_AFFECTED    NOT_AFFECTED    PASS
RWB-07   NOT_AFFECTED    UNKNOWN         FAIL (known)
RWB-08   AFFECTED        AFFECTED        PASS
RWB-09a  AFFECTED        AFFECTED        PASS
RWB-09b  NOT_AFFECTED    NO_FINDING      FAIL (known)
RWB-10   UNKNOWN         UNKNOWN         PASS
RWB-11a  AFFECTED        AFFECTED        PASS
RWB-11b  NOT_AFFECTED    NOT_AFFECTED    PASS
VAL-001  AFFECTED        AFFECTED        PASS
VAL-002  AFFECTED        UNKNOWN         FAIL (known)
VAL-003  NOT_AFFECTED    UNKNOWN         FAIL (known)
```

## Known failures (tracked)

- RWB-03: Real-world benchmark RWB-03 (METHOD): parseFeed() constructs a real XMLParser instance with htmlEntities:true and calls its vulnerable .parse() method on caller-supplied XML text.
  - parseFeed() unconditionally constructs `new XMLParser({processEntities:true, htmlEntities:true})` and calls `.parse(xmlText)` on it -- the exact option shape the advisory's own PoC uses. Live-reproduced against the real installed package before this oracle was written (RangeError: Invalid code point 9999999). A human analyst reading this file has no ambiguity: this is a real, reachable call to the vulnerable instance method. Currently returns UNKNOWN instead -- a known, tracked gap, not a fabricated oracle. See FINDINGS.md RWF-006 (webpack-bundled getter-defined class exports aren't recognized as constructible/method-bearing).
- RWB-05: Real-world benchmark RWB-05 (UNUSED_API): the application imports qs and calls only the unrelated, unaffected qs.stringify() export -- the vulnerable qs.parse() export is never referenced.
  - toQueryString() calls qs.stringify(filters) only. qs.parse (the export GHSA-hrpp-h998-j3pp/CVE-2022-24999 names as vulnerable) is never imported or called anywhere in this application's source -- confirmed by reading the real installed lib/index.js, which exports parse and stringify as two independent named exports of the same module. A human analyst reading this file has no ambiguity: this vulnerable export is never reached. Currently returns UNKNOWN instead, and even the SAFE call (qs.stringify) fails to resolve -- see FINDINGS.md RWF-004 (module.exports referencing values imported from sibling files isn't chased).
- RWB-07: Real-world benchmark RWB-07 (entrypoint-unreached): ini.parse() is genuinely called in this source, but only from a function never reached by the configured {file, symbol} entrypoint.
  - loadLegacyIniConfig() has a real, live, unconditional call to ini.parse() -- a naive grep for 'calls the vulnerable function' would wrongly flag it. But the configured entrypoint is {file: src/config.js, symbol: loadModernConfig}; loadLegacyIniConfig is not itself an entrypoint source, is never called by loadModernConfig, and is never called by anything else in this file. A human analyst reading this file, informed of the actual entrypoint, concludes with confidence this code path is not reachable from the application's real usage. Currently returns UNKNOWN instead -- a known, tracked gap, not a fabricated oracle. Real ini@1.3.5 exports 'parse' via a chained CommonJS alias (exports.parse = exports.decode = decode); the implementing function is named 'decode', not 'parse', so export-symbol attribution cannot resolve the rule's ini#parse target to any real function at all -- target attribution is correctly reported unresolved (UNKNOWN) rather than risking a coincidental match. Before VT-301B this same unresolved target fell through to a phantom node, which happened to read as NOT_AFFECTED only because loadModernConfig's own reachable subgraph is trivially edge-less -- not because the target was ever actually identified. See FINDINGS.md RWF-012.
- RWB-09b: Real-world benchmark RWB-09b (MULTI_INSTANCE, patched instance): the patched semver@7.5.2 instance, the application's plain direct dependency, reached via isCompatible().
  - isCompatible() calls `new semver.Range(userRange)`, where semver is `require('semver')` -- the real, patched semver@7.5.2 (>=7.5.2, outside GHSA-c2qf-rxjj-qqgw/CVE-2022-25883's vulnerable range). Same class, same rule, same file as RWB-09a -- the only difference is which real installed instance is reached from which entrypoint function. A human analyst reading this file and the real package-lock.json has no ambiguity: this specific installed instance is patched. IMPORTANT -- this is a BENCHMARK DESIGN ISSUE, not a VulnTrace defect: the actual scan result is NO_FINDING (no findings entry at all for this instance), not an explicit NOT_AFFECTED verdict, because VulnTrace's buildFinding() correctly omits any finding when matchResult is confidently "not_affected" by version alone (verdict.ts's documented behavior). This case's expected value should arguably represent "no finding" rather than a verdict string; left unchanged here per instructions not to silently alter the oracle -- see FINDINGS.md's note on RWB-09b.
- VAL-002: The real, npm-installed lodash@4.17.15's trim() is called directly on unsanitized input from the entrypoint.
  - sanitizeField() calls _.trim(rawValue) directly -- trim is one of the three functions (trim/trimEnd/toNumber) GHSA-29mw-wpgm-hmr9/CVE-2020-28500 names as vulnerable to ReDoS. A human analyst reading this file has no ambiguity: this is reachable. Currently returns UNKNOWN instead -- a known, tracked gap, not a fabricated oracle. See FINDINGS.md RWF-001.
- VAL-003: The real, npm-installed lodash@4.17.15 is used by the entrypoint, but only via chunk()/uniq() -- never trim, trimEnd, or toNumber.
  - groupUniqueIds() never calls trim, trimEnd, or toNumber. A human analyst reading this file concludes with confidence this code path is not exposed to GHSA-29mw-wpgm-hmr9/CVE-2020-28500, regardless of the installed version being in the vulnerable range. Currently returns UNKNOWN instead, for the same structural reason as VAL-002 -- see FINDINGS.md RWF-001. This case demonstrates the gap degrades precision in BOTH directions: a missed AFFECTED (VAL-002) and a missed NOT_AFFECTED (this case), never a false answer either way.

