node_modules/
dist/
coverage/
fixtures/
# Adversarial test fixtures deliberately contain unusual/invalid-looking
# code -- same treatment as fixtures/ above.
tests/adversarial/*/fixtures/
# Real, vendored third-party npm packages -- not project code.
tests/validation/fixtures/

# Pre-existing spec/task/script content, out of scope for the bootstrap
# TypeScript project (TASK-001 only owns src/ + root tool config).
docs/
schemas/
rules/
config/
scripts/
*.md
