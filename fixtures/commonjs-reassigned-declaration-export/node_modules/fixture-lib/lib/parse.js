"use strict";

// The real, vulnerable implementation. Anonymous, so its only identity is
// its own position in this file -- and so nothing in this package other
// than the stale declaration carries the name "parse".
module.exports = function (input) {
  return "danger:" + String(input);
};
