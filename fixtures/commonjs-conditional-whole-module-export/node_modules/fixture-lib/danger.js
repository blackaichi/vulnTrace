"use strict";

// The vulnerable sink the advisory names. Reachable at runtime whenever
// `FIXTURE_LIB_MODE === "fast"` selects `dangerousOp` as the package's
// exported value.
function explode(input) {
  return "danger:" + input;
}

exports.explode = explode;
