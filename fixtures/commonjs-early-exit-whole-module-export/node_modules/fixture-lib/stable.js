"use strict";

// The Family C positive control, in the same package as the two ambiguous
// modules above. Its whole-module export is a single UNCONDITIONAL
// top-level assignment with NO abrupt completion anywhere above it, so its
// identity is a fact: `neverCalled` IS this module's exported value.
//
// The control entrypoint requires this module and never calls it, so the
// target is genuinely unreachable and NOT_AFFECTED here is a real proof --
// it must survive RWF-015 untouched.
function neverCalled(input) {
  return "stable:" + input;
}

module.exports = neverCalled;
