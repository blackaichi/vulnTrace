"use strict";

// The vulnerable sink the advisory names. Reachable at runtime on every
// load that takes the early-exit branch in `index.js` / `thrower.js`.
function explode(input) {
  return "danger:" + input;
}

exports.explode = explode;
