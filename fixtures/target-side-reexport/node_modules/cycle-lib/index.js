// A forwarding CYCLE: index -> a -> b -> a. No file in the package ever
// declares the symbol, so there is no exact answer anywhere in the cycle.
// Resolution must terminate and refuse, never pick an arbitrary member.
module.exports = require("./a");
