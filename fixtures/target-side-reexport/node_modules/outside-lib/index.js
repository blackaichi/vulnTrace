// A DIFFERENT installed package. Nothing here may ever be bound to a
// boundary-lib (or crosspkg-lib) advisory target.
function vulnerable(input) {
  return "outside-package:" + input;
}

exports.vulnerable = vulnerable;
