// UNSUPPORTED forwarding: the specifier is computed, so no authoritative
// re-export origin exists and no hop may be taken. Must degrade to an
// unresolved target, never to a guess and never to a negative verdict.
var which = process.env.IMPL_NAME || "./impl";

module.exports = {
  vulnerable: require(which).internalName,
};
