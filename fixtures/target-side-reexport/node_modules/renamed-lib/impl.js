// The REAL target, declared under its implementation-facing name.
function internalName(input) {
  return "renamed-vulnerable:" + input;
}

// A DECOY that is literally named `vulnerable` and lives in the very file
// the rename points at. It is exported under a different name, so nothing
// the package publishes as "vulnerable" is this function. Any resolution
// that searched a file for a same-named declaration would bind here.
function vulnerable(input) {
  return "renamed-decoy:" + input;
}

exports.internalName = internalName;
exports.unrelatedDecoy = vulnerable;
