// A RENAMED property re-export: the advisory-facing name is "vulnerable",
// the implementation-facing name it actually selects is "internalName".
// Resolution must carry that mapping, never search the implementation file
// for something literally called "vulnerable".
exports.vulnerable = require("./impl").internalName;
