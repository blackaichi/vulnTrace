// The only declaration in the package. Published as the module's canonical
// "default" export under its own, different, implementation-facing name.
module.exports = function twoHopImplementation(input) {
  return "twohop-vulnerable:" + input;
};
