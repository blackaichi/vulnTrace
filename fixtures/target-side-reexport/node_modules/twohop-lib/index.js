// Hop 1 of 2. A barrel entry whose advertised `vulnerable` is the WHOLE
// value of a sibling barrel -- no file in this package ever declares or
// exports anything under the literal name "vulnerable".
var api = require("./api");

module.exports = {
  vulnerable: api,
};
