// The SAME implementation, published under TWO names. An advisory naming
// `vulnerable` describes the same concrete callable the application
// reaches by calling `alsoVulnerable` -- so calling either one really does
// execute the advisory's target.
var impl = require("./impl");

module.exports = {
  vulnerable: impl,
  alsoVulnerable: impl,
};
