// NESTED twin instance -- identical name and version to the top-level one,
// different install path, different implementation.
var impl = require("./impl");

module.exports = {
  vulnerable: impl,
};
