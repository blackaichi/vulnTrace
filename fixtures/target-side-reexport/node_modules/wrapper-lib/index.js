// Reaches the NESTED twin-lib install (its own node_modules), never the
// top-level one -- exactly Node's own resolution.
var twin = require("twin-lib");

exports.run = function run(input) {
  return twin.vulnerable(input);
};
