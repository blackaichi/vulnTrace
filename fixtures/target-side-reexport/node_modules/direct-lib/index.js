// CONTROL: the advisory's symbol is declared and exported by the package's
// own entry file, with no forwarding layer at all. Target-side re-export
// resolution must never be needed -- nor change anything -- here.
function vulnerable(input) {
  return "direct:" + input;
}

exports.vulnerable = vulnerable;
