// qs@6.10.1's exact public-entry shape: nothing is declared here. Each
// advertised export is a local var bound to a whole-module require of a
// sibling file, published through an object literal.
var impl = require("./impl");
var safe = require("./safe");

module.exports = {
  vulnerable: impl,
  safe: safe,
};
