// The unrelated, unaffected sibling export -- qs's `stringify` to the
// `parse` above. Calling this must never make the vulnerable target
// reachable.
module.exports = function (input) {
  return "onehop-safe:" + input;
};
