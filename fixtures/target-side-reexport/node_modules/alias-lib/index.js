// A LOCAL ALIAS between the require and the export write -- single
// assignment, never reassigned, so the binding's value is a fact.
var impl = require("./impl");
var alias = impl.internalName;

module.exports.vulnerable = alias;
