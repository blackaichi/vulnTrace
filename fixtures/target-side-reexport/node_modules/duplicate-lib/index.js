// DUPLICATE EXPORT WRITES, last-write-wins (Node's real semantics). The
// first write names the dangerous implementation and is then overwritten;
// what this package actually publishes as "vulnerable" is `harmless`.
// Preserving the stale FIRST write would be a false target.
exports.vulnerable = require("./impl").dangerous;
exports.vulnerable = require("./impl").harmless;
