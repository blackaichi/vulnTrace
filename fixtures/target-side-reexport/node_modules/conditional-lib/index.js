// UNSUPPORTED forwarding: the export write is CONDITIONAL, so which value
// the package publishes under the advisory's name is not a fact about the
// file. Must degrade to an unresolved target.
var impl = require("./impl");

if (process.env.MODE === "fast") {
  module.exports.vulnerable = impl.fastPath;
} else {
  module.exports.vulnerable = impl.slowPath;
}
