// PACKAGE-ROOT BOUNDARY: a RELATIVE forwarding specifier that escapes this
// package's own root and lands inside a different installed package. The
// advisory names boundary-lib; an implementation in outside-lib is not
// boundary-lib's target, so this hop must be refused rather than silently
// re-pointing the advisory at another package's code.
module.exports = {
  vulnerable: require("../outside-lib").vulnerable,
};
