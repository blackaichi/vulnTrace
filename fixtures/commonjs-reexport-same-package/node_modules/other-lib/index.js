// A DIFFERENT installed package that happens to export a function with the
// exact same name as fixture-lib's. Nothing here may ever be bound to a
// `fixture-lib#vulnerable` rule target.
function vulnerable(input) {
  return "other-package-vulnerable:" + input;
}

exports.vulnerable = vulnerable;
