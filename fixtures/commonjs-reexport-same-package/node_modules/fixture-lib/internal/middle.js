// The middle hop of the same-package chain: it re-exports the whole
// sibling module, so the facade's `middle.vulnerable` must forward through
// here and land on ../lib.js's real declaration.
module.exports = require("../lib");
