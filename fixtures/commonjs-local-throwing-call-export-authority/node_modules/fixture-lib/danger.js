"use strict";

// The vulnerable sink the advisory names. Reachable at runtime on every
// load that takes the early branch in `index.js`, because that branch
// exports `dangerousOp` before the resolvable local throwing call runs.
function explode(input) {
  return "danger:" + input;
}

exports.explode = explode;
