// THE public entry -- package.json declares "main": "lib/entry.js". The
// file literally named index.js at the package root is NOT the entry, and
// is deliberately loaded anyway (below) so that its same-named export is a
// live candidate for any filename-based or per-file attribution.
exports.vulnerable = require("./impl").realImpl;
exports.runDecoy = require("../index.js").vulnerable;
