// A file named index.js at the package root that is NOT this package's
// public entry. Assuming `index.js` universally, instead of asking the
// package's own metadata, would bind the advisory here.
exports.vulnerable = function mainfieldIndexDecoy(input) {
  return "mainfield-decoy:" + input;
};
