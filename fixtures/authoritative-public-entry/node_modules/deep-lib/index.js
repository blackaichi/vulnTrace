// The public entry publishes NOTHING called `vulnerable`. The consumer
// deep-imports `deep-lib/deep` and calls that file's own `vulnerable`.
// An advisory naming the PUBLIC `deep-lib#vulnerable` is a different
// claim from "some file in deep-lib exports that name", so this stays
// UNKNOWN -- see the fixture README's deep-import boundary note.
exports.safe = function deepLibSafe(input) {
  return "deep-safe:" + input;
};
