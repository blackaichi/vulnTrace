// An unrelated SIBLING that genuinely exports the advisory's literal name.
// It is NOT what the package publishes as `vulnerable` -- the public entry
// re-publishes it under the name `runOther`. Binding an advisory naming
// `publicsafe-lib#vulnerable` here is the false AFFECTED RWF-030 is about.
exports.vulnerable = function dangerousSibling(input) {
  return "publicsafe-dangerous:" + input;
};
