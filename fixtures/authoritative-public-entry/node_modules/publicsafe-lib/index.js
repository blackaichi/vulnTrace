// THE canonical RWF-030 shape. This file is the package's PUBLIC ENTRY
// (package.json "main"), and it is the only authority on what
// `publicsafe-lib#vulnerable` names. What it publishes under `vulnerable`
// is impl.js's safeImpl. The dangerous callable in other.js is published
// under a DIFFERENT public name, so `pkg.vulnerable` can never reach it.
var impl = require("./impl");
var other = require("./other");

module.exports = {
  vulnerable: impl.safeImpl,
  runOther: other.vulnerable,
};
