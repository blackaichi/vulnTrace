// The reverse control: the PUBLIC ENTRY really does publish the dangerous
// implementation under the advisory's name, through a rename hop. A
// same-named SAFE sibling must not shadow it.
exports.vulnerable = require("./impl").dangerousImpl;
exports.runSafeSibling = require("./safe").vulnerable;
