// Same literal name, harmless callable, not what the entry publishes as
// `vulnerable`. It must never shadow the authoritative public export.
exports.vulnerable = function harmlessSibling(input) {
  return "publicvuln-harmless:" + input;
};
