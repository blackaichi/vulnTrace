// Public RENAME onto a safe implementation, with a same-named dangerous
// sibling present. The target must be impl.js's safeInternal -- and since
// the consumer never calls it, the verdict must be a genuine negative, not
// an AFFECTED pointing at other.js.
exports.vulnerable = require("./impl").safeInternal;
exports.runOther = require("./other").vulnerable;
