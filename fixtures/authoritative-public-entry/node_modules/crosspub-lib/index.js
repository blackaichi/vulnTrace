// The public entry forwards the advisory's name into a DIFFERENT package.
// P1-A1's boundary is preserved: an advisory naming `crosspub-lib` is not
// silently re-pointed at an implementation owned by `outside-lib`.
exports.vulnerable = require("outside-lib").vulnerable;
exports.runOther = require("./other").vulnerable;
