// The public entry publishes the dangerous implementation EXACTLY, under
// the advisory's own name -- and the consumer never calls it. Exact
// authoritative resolution must not, by itself, force AFFECTED: this case
// must still receive a genuine negative proof.
exports.vulnerable = require("./impl").dangerousImpl;
exports.safe = require("./safe").safeImpl;
