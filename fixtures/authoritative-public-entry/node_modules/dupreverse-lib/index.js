// The mirror of dupwrite-lib: the DANGEROUS write is the last one, so it
// is the one the package really publishes. Source order, not a preference
// for whichever value looks safer, decides.
exports.vulnerable = require("./safe").safeImpl;
exports.vulnerable = require("./dangerous").dangerousImpl;
exports.runOther = require("./other").vulnerable;
