// Direct public export control: the public entry IS the implementation
// file. No indirection is required, and none must be invented. A
// same-named sibling exists anyway.
exports.vulnerable = function vulnerableImpl(input) {
  return "directpub-vulnerable:" + input;
};

exports.runOther = require("./other").vulnerable;
