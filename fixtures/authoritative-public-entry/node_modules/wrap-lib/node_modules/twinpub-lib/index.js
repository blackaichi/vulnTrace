// NESTED twin -- name- and version-identical to the top-level install, but
// a different PackageInstance whose public entry publishes the DANGEROUS
// implementation under `vulnerable`.
exports.vulnerable = require("./impl").dangerousImpl;
exports.runOther = require("./other").vulnerable;
