// Reaches the NESTED twinpub-lib install, which is a different
// PackageInstance from the top-level one of the same name and version.
var nested = require("twinpub-lib");

exports.runNestedVulnerable = function runNestedVulnerable(input) {
  return nested.vulnerable(input);
};
