// The negative control: the public entry does NOT publish `vulnerable` at
// all, under any name. A sibling does. The advisory names
// `missing-lib#vulnerable`, and the only sound answer is UNRESOLVED.
var other = require("./other");

exports.safe = function safePublic(input) {
  return "missing-safe:" + input;
};

// `other` is genuinely loaded (so its file is in the graph and the legacy
// per-file scan would have found its `vulnerable`), but nothing about it
// is published under the advisory's name.
exports.otherModuleName = typeof other.vulnerable;
