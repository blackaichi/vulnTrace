// Three unrelated siblings all export the advisory's literal name. Only
// the public entry's own forwarding chain decides the target, so no
// traversal order over the package's files can change the answer.
exports.vulnerable = require("./impl").internalName;
exports.runOne = require("./sibling-a").vulnerable;
exports.runTwo = require("./sibling-b").vulnerable;
exports.runThree = require("./sibling-c").vulnerable;
