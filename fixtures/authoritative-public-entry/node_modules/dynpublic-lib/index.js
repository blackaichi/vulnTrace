// A DYNAMIC/computed public export name. The public surface does not
// statically prove which name the implementation is published under, so
// the advisory's `vulnerable` cannot be bound authoritatively -- UNKNOWN,
// with no sibling-scan rescue.
var impl = require("./impl");
var key = process.env.AUTHORITATIVE_PUBLIC_ENTRY_KEY || "vulnerable";

exports[key] = impl.dangerousImpl;
exports.runOther = require("./other").vulnerable;
