// TOP-LEVEL twin. Same name AND version as the copy nested under
// wrap-lib, but its public entry publishes the SAFE implementation under
// `vulnerable`, with a dangerous same-named sibling present.
exports.vulnerable = require("./impl").safeImpl;
exports.runOther = require("./other").vulnerable;
