// ESM public entry: an explicit renaming re-export. The same-named
// function declared in other.js is not what this package publishes as
// `vulnerable`.
export { internalName as vulnerable } from "./impl.js";
export { vulnerable as runOther } from "./other.js";
