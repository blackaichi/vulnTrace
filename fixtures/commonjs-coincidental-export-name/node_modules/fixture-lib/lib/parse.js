"use strict";

// The real, vulnerable implementation. Anonymous, so its only identity is
// its own position in this file -- and so no node anywhere in the package
// carries the name "parse" except the decoy in index.js.
module.exports = function (input) {
  return "danger:" + String(input);
};
