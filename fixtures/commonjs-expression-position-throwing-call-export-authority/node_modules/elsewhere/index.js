"use strict";

// Pulls in the same-name, same-version TWIN of `fixture-lib` from its own
// nested install, so both instances are genuinely discovered by the call
// graph and their identities have to be told apart by install LOCATION.
const twin = require("fixture-lib");

module.exports = function useTwin(input) {
  return twin(input);
};
