"use strict";

// The vulnerable sink the advisory names. Reachable at runtime on every
// load that takes the early branch, because that branch exports
// `dangerousOp` before the invocation that ends module evaluation.
function explode(input) {
  return "danger:" + input;
}

exports.explode = explode;
