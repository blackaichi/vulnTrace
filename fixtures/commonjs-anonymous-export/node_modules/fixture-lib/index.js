"use strict";

// The facade declares NOTHING: the package's whole callable API comes from
// a require()d sibling inside this same installed package (RWF-004a's
// relation), and bottoms out in an ANONYMOUS function assigned straight to
// module.exports (RWF-003's relation).
module.exports = require("./internal/middle");

// A DIFFERENT installed package's anonymous export, hung off the same
// exported value. Chasing this is cross-package re-export (RWF-004b),
// which is not implemented.
module.exports.fromOtherPackage = require("other-lib");
