"use strict";

// A DIFFERENT installed package whose whole API is also an anonymous
// module.exports function. Reaching it from fixture-lib's facade would be
// cross-package re-export (RWF-004b), which is not implemented.
module.exports = function (input) {
  return "other-package:" + input;
};
