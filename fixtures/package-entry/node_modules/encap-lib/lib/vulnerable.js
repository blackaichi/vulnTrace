// Internal, and NOT published by `exports`. It is loaded and reachable,
// but `require("encap-lib/lib/vulnerable")` throws under real Node, so it
// is not an authoritative public surface of this package.
exports.vulnerable = function encapDanger(input) {
  return "encap-internal:" + input;
};
