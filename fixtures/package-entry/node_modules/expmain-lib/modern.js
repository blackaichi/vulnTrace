// The package's PUBLIC entry: package.json "exports" -> "." -> this file.
// Node gives `exports` authority over `main`, so `require("expmain-lib")`
// is this file, and legacy.js is not part of the public surface at all.
exports.vulnerable = function modernDanger(input) {
  return "expmain-modern:" + input;
};
