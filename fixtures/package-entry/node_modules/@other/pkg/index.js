// A DIFFERENT package that shares the basename `pkg`. Splitting a scoped
// specifier wrongly (at "@other" instead of "@other/pkg") would make the
// scope DIRECTORY look like the package root.
exports.vulnerable = function otherScopeDanger(input) {
  return "other-scope:" + input;
};
