// A root index.js that "main" points AWAY from. `index.js` is a fallback
// convention, never an override of an explicit "main".
exports.vulnerable = function mainonlySibling(input) {
  return "mainonly-sibling:" + input;
};
