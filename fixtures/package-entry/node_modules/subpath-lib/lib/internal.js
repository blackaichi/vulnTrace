// Not exported by package.json "exports" at all. Under real Node,
// `require("subpath-lib/lib/internal")` throws ERR_PACKAGE_PATH_NOT_EXPORTED.
exports.vulnerable = function internalDanger(input) {
  return "subpath-internal:" + input;
};
