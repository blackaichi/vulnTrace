// The "." surface. Its `vulnerable` is a DIFFERENT callable from the one
// `subpath-lib/parse` publishes under the same name, and the consumer
// never calls it.
exports.vulnerable = function rootSafe(input) {
  return "subpath-root-safe:" + input;
};
