// Physically OUTSIDE escape-lib's own package root. An `exports` mapping
// must never be allowed to reach it.
exports.vulnerable = function escapeOutsideDanger(input) {
  return "escape-outside:" + input;
};
