// The ALIAS install (`"twin-alias": "npm:twin-lib@1.0.0"`): same manifest
// NAME, same version, a different PackageInstance. Its public `vulnerable`
// is the dangerous, called one.
exports.vulnerable = function aliasDanger(input) {
  return "twin-alias-danger:" + input;
};
