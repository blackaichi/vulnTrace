// The "import" branch -- dangerous, but a CommonJS consumer never loads it.
// A candidate from an inactive condition must materialize NOTHING: it can
// only contribute a target when the call graph holds a real node for it.
export function vulnerable(input) {
  return "condsafe-esm-dangerous:" + input;
}
