// A root `index.js` sibling exporting the same literal name. `index.js` is
// a CONVENTION, not a rule: with `exports` declared, this file is not the
// package's public entry and is unreachable through the package name.
exports.vulnerable = function shorthandSibling(input) {
  return "shorthand-sibling:" + input;
};
