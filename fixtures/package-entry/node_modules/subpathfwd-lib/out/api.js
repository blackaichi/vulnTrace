// The "./api" public surface. It FORWARDS the advisory's name rather than
// declaring it, so the target resolves through P1-A1's forwarding relation
// -- anchored here, at the subpath's own authoritative entry.
exports.vulnerable = require("./impl").vulnerable;
