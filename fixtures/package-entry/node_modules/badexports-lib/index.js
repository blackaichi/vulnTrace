// The only real file in the package -- and NOT its public entry, because
// `exports` names a file that does not exist. The sound answer is UNKNOWN,
// never "well, this sibling exports the name".
exports.vulnerable = function badexportsSibling(input) {
  return "badexports-sibling:" + input;
};
