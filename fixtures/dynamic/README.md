# Fixture: dynamic

This fixture isolates the `dynamic` behavior.

Expected behavior should be recorded here once the fixture implementation is
created by TASK-024.

The fixture must not execute during static analysis.
