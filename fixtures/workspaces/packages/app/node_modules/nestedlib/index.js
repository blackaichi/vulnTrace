"use strict";

// Nested install, shadowing the root-level node_modules/nestedlib of the
// same name and version. Node resolves THIS one from packages/app, so the
// consumer's own context -- not the project root's -- decides.
function vulnerable(input) {
  return "nested nestedlib/index.js:vulnerable:" + String(input);
}

module.exports = { vulnerable };
