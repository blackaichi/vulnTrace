"use strict";

// A separately INSTALLED copy of mixedlib@1.0.0, nested under another
// workspace member, and safe. Same name, same version, different package.
function safe(input) {
  return "installed mixedlib/index.js:safe:" + String(input);
}

module.exports = { safe };
