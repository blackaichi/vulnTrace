"use strict";

// A REAL, separately INSTALLED copy of `twinlib@1.0.0` -- not a link to
// the workspace package of the same name and version. Its implementation
// is the dangerous one. Physically distinct root => distinct
// PackageInstance, no matter how identical the manifests are.
function vulnerable(input) {
  return "installed twinlib/index.js:vulnerable:" + String(input);
}

module.exports = { vulnerable };
