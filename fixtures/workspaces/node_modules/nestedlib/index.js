"use strict";

// The root-level copy, shadowed from packages/app by the nested one. Safe,
// so attributing the nested consumer's call to THIS instance would be a
// false NOT_AFFECTED.
function safe(input) {
  return "root nestedlib/index.js:safe:" + String(input);
}

module.exports = { safe };
