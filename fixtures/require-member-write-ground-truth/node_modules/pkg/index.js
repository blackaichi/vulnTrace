"use strict";

// The loud-fixture rule (RWF-048 § 2): this package exports EVERY name the
// RWF-047 cases bind, INCLUDING `patched`. Against a package that does not
// export the name a stale attribution degrades to `unresolved_target` and
// reads as an honest UNKNOWN -- the hiding mechanism that concealed the
// RWF-046 array hole behind a green suite.

const calls = [];

function run() {
  calls.push("pkg#run");
  return "pkg#run";
}
function patched() {
  calls.push("pkg#patched");
  return "pkg#patched";
}
function execute() {
  calls.push("pkg#execute");
  return "pkg#execute";
}
function safe() {
  calls.push("pkg#safe");
  return "pkg#safe";
}

module.exports = { run, patched, execute, safe, calls };
