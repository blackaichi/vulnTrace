"use strict";

// The package's whole callable API is chosen AT RUNTIME by a resolvable
// LOCAL CALL whose own body always throws -- but, unlike RWF-016's
// fixture, the call is NOT a bare `bail();` expression statement. It is
// the INITIALIZER of a variable declaration, which JavaScript evaluates
// as part of executing the declaration. The runtime consequence is
// identical (see fixtures/commonjs-circular-import-initializer-throw-
// ground-truth/ for a real node-executed proof); only the syntactic
// position of the CallExpression differs, which is exactly what RWF-017
// says must not matter.
const danger = require("./danger");

// The branch that reaches the vulnerable sink. Nothing in this file calls
// it: the ONLY way it ever runs is by being the module's exported value.
function dangerousOp(input) {
  return danger.explode(input);
}

// The branch that does not.
function safeOp(input) {
  return "safe:" + input;
}

// A resolvable local callable whose entire body is a single unconditional
// throw -- definitely abrupt by RWF-016's own model.
function bail() {
  throw new Error("fast mode is not supported here");
}

if (process.env.FIXTURE_LIB_MODE === "fast") {
  module.exports = dangerousOp;
  // The declaration's initializer is evaluated when the declaration
  // executes, so this necessarily invokes `bail()`. It never returns, the
  // declaration never completes, `result` is never bound, and module
  // evaluation on this branch ends here.
  const result = bail();
}

module.exports = safeOp;
