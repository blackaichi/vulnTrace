"use strict";

// PackageInstance identity under a computed key.
//
// Two installs of nested-vuln@1.0.0 exist in this fixture -- the top-level
// node_modules/nested-vuln and this package's own private
// node_modules/fixture-lib/node_modules/nested-vuln. They are identical in
// name, version and vulnerable export name, and distinguishable only by
// install path. Node resolves the require below to the NESTED one.
//
// Adding a class-definition-time edge must not become a licence to answer
// for the twin: the top-level install is not what this key reaches, and a
// finding scoped to it must not turn AFFECTED.
const nested = require("nested-vuln");

function key() {
  nested.dangerousOp("from-computed-key");
  return "x";
}

class C {
  [key()]() {}
}

module.exports = C;
