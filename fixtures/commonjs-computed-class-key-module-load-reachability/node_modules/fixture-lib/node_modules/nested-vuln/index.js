"use strict";

// The vulnerable export. This file exists twice, at two different install
// paths, with identical name, version and export name.
function dangerousOp(input) {
  return "danger:" + input;
}

exports.dangerousOp = dangerousOp;
