"use strict";

// The ISOLATING CONTROL for index.js. Byte for byte the same call, the
// same wrapper, the same sink -- moved out of the computed key and into a
// plain top-level ExpressionStatement.
//
// This was already AFFECTED before RWF-023, and index.js was
// NOT_AFFECTED. The only semantic difference between the two files is the
// POSITION of the call, which is what makes this pair a proof that the
// defect was one of call-position modeling and nothing else.
const danger = require("./danger");

function key() {
  danger.explode("from-top-level");
  return "x";
}

key();

class C {
  ["x"]() {}
}

module.exports = C;
