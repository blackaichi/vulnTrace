"use strict";

// Static field initializer / static block hosts for a nested class.
const fixture = require("fixture-lib/class-definition-time");

module.exports = function main(input) {
  return { fixture, input };
};
