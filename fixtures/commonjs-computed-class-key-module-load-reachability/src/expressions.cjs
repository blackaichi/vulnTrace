"use strict";

// Key EXPRESSION shapes: wrappers, nesting, short-circuits, class expressions.
const fixture = require("fixture-lib/expressions");

module.exports = function main(input) {
  return { fixture, input };
};
