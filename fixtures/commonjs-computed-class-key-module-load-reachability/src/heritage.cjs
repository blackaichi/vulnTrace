"use strict";

// Valid and invalid heritage values, each carrying a computed key.
const fixture = require("fixture-lib/heritage");

module.exports = function main(input) {
  return { fixture, input };
};
